{
  "name": "web-ai-project-root",
  "version": "1.0.0",
  "scripts": {
    "frontend-install": "npm install --prefix Frontend",
    "frontend-build": "npm run build --prefix Frontend",
    "build": "npm run frontend-install && npm run frontend-build && mv dist public"
  },
  "devDependencies": {
    "tailwindcss": "latest"
  },
  "dependencies": {
    "@tailwindcss/cli": "latest",
    "@tailwindcss/vite": "latest"
  }
}